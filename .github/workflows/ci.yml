name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: aos-ce-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  format:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.94"
          components: rustfmt
      - run: cargo fmt --all -- --check

  astrid-wasm:
    name: Astrid WASM
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.94"
          components: clippy
          targets: wasm32-unknown-unknown
      - uses: Swatinem/rust-cache@v2
      - run: cargo metadata --locked --format-version 1 --no-deps
      - run: cargo check --locked --workspace --exclude astrid-capsule-telegram --target wasm32-unknown-unknown
      - run: cargo clippy --locked --workspace --exclude astrid-capsule-telegram --target wasm32-unknown-unknown -- -D warnings

  telegram-wasi:
    name: Telegram WASI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.94"
          targets: wasm32-wasip1
      - uses: Swatinem/rust-cache@v2
      - run: cargo check --locked -p astrid-capsule-telegram --target wasm32-wasip1
